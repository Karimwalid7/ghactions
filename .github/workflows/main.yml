name: First Workflow 

on: 
 push:
  branches: 
     - main

jobs: 
  example-job:
    runs-on: ubuntu-latest 

    steps: 
      - name: print a welcome message 
        run: echo "Welcome to the fist workflow"

      - name: Checkout repository
        uses: actions/checkout@v5

      - name: List repository files 
        run: ls -la 
